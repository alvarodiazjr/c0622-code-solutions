/* exported zip */
function zip(first, second) {
}

// if (first.length <= second.length) {
//   var zip = first.map(function (lady, i) {
//     return [lady, second[i]];
//   });
// }
// return zip;

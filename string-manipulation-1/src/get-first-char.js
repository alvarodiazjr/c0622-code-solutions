/* exported getFirstChar
-need a way to access the first character of the string
-return the value of the first character.
*/
function getFirstChar(string) {
  var firstChar = string.charAt(0);
  return firstChar;
}

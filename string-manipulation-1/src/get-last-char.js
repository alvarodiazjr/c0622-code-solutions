/* exported getLastChar
-need a way to access the last character of the string
-need to return just that character.
*/
function getLastChar(string) {
  var lastChar = string.charAt(string.length - 1);
  return lastChar;
}

/* exported getWords
- need a way to split up the string into an array.
- split the string if theres any spaces.
- return the value of the string but into an array.
*/
function getWords(string) {
  return string.split(' ', string.length);
}
